{%- for block in section.blocks -%}
    {% render block %}
{%- endfor -%}

{% schema %}
{
    "name": "t:sections.apps.name",
    "tag": "section",
    "class": "section",
    "blocks": [
        {
            "type": "@app"
        }
    ],
    "presets": [
        {
            "name": "t:sections.apps.presets.name"
        }
    ]
}
{% endschema %}
